<h1>Good news!</h1>

<p><a href="<%= product_detail_url(id: @product.id, slug: @product.slug) %>"><%= @product.name %> is back in stock.</a></p>

<%= link_to "Unsubscribe", unsubscribe_url(token: params[:subscriber].generate_token_for(:unsubscribe)) %>
